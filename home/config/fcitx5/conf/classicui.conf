# fcitx5 候选框外观：单独加大字体，不影响全局 GTK
# X11 下 fcitx5 classicui 用 GTK 渲染，会继承 gtk-font-name
# 这里覆盖为更大的字体，使用原生 CJK 字体确保 rime 候选框正常渲染
[ClassicUI]
Font="Noto Sans CJK SC 16"
VerticalPadding=4
HorizontalPadding=8
UseAntiAlias=True
PerScreenDPI=True
