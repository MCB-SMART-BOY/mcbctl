# Existing profile modules use explicit dotted assignments for Home Manager options.
# Keep these style diagnostics visible during later cleanup, but do not fail the check.
disabled = [
  "manual_inherit",
  "manual_inherit_from",
  "empty_pattern",
  "repeated_keys",
  "useless_has_attr",
]
